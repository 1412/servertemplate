var sequelizetask = [
    /*
    {
        ref: "", // task name for referencing
        task: "", // create (build then save), build, save, relate, update delete,
        table: "", // table name associated,
        continueOnError: false, // should it continue if error occured
        data: {}, // table data or new data if task is build or create
        use: "", // if task is relate, call object created from task_reference for source
        set: "", // if task is relate, the relation field the sorce object will be related
        dataref: [], // if task is relate, one or more object created from task_referecne will related
    }
    */
]

module.exports = sequelizetask;